ssh bandit5@bandit.labs.overthewire.org -p 2220 in the root terminal
bandit 4->5 after entering the password

enter "ls -alps" it will show the all the files and folder to see the human readable file 
enter the "find . -type f | xargs file" it will human readable file "ASCII TEXT"

THEN "cat ./-file7"
