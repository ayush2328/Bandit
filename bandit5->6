ssh bandit6@bandit.labs.overthewire.org -p 2220 in the root terminal
enter the password then 
enter "ls -alps"
cd inhere
NOTE: Because the file in the inside inhere folder and its is only human readable , size 1033 bytes (represted by 1033c) and not executable file . There are many folder inside "inhere folder" so then to find this file....
enter "find . -type f -size 1033c ! -executable"
enter "cat ./maybehere07/.file2"
then copy the password "Ctrl + Shift + C"
then exit